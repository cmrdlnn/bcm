#### Запуск приложения

*   `vagrant up`
*   `vagrant ssh`
*   `bundle install`
*   `make run`

#### Переменные окружения

##### Остальные переменные окружения

*   `BCM_LOG_LEVEL` — режим отображения событий
